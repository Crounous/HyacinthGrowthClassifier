# Backend service configuration
# Absolute path inside the container/host where the model checkpoint will be stored.
MODEL_PATH=/tmp/best_model.pth

# OPTIONAL: Publicly accessible URL to download `best_model.pth` when it is missing.
# Leave blank if the model is baked into the deployment image already.
MODEL_URL=https://drive.google.com/uc?id=1_7GG84XkQ0-gX-VYEyOnoWVf3RBTM6mj

# Frontend (Vite) configuration
# Local development default for the API base URL. Override per-environment as needed.
VITE_API_URL=http://localhost:8000
